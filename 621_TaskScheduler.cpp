#include <vector>

using namespace std;

class Solution {
public:
    int leastInterval(vector<char>& tasks, int n) {
        if (0 == n) // better coding style for compiler debugging than n == 0
            return tasks.size();
        
    }
};